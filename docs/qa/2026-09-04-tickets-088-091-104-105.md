# Tickets 088, 091, 104, and 105 — local execution

Local implementation is ready for hosted acceptance. No hosted migration,
deployment, mailbox rule, email, or real push notification was performed.

## Ticket outcomes

| Ticket | State | Evidence and remaining work |
| --- | --- | --- |
| 088 | Complete locally | Behavior creation reads the current profile timezone. Sync summaries use actual generation windows. Existing state-version and configuration guards reject stale workers. |
| 091 | In progress | Export opens with aggregate counts. Explicit Markdown generation and downloads retain guardrails. Five-minute cron is configured; the existing Pro plan supports it. Hosted migration and delivery timing remain unverified. |
| 104 | Blocked | Prior synthetic delivery reached a filtered folder. Root cause remains unverified. Operator diagnostics and one separately approved synthetic test are required. Private vulnerability reporting remains enabled. |
| 105 | In progress | web-push 3.6.7 remains the latest published package. The upstream exception names an owner and recheck trigger in OPERATIONS.md. Authorized Preview inspection remains required. |

## Implementation

- `lib/services/behavior.service.ts` bypasses cached and submitted timezone values during creation.
- `packages/core/src/services/occurrence-sync.ts` records the timezone used by generation windows.
- `supabase/migrations/20260905020741_add_export_page_summary.sql` adds an authenticated counts-only read with RLS and explicit owner predicates.
- `lib/db/exportPageRead.repo.ts` validates counts returned by the summary RPC.
- `lib/services/export.service.ts` separates page reads from guarded artifact generation.
- `components/export/ExportPanel.tsx` generates Markdown on request and clears it when options change.
- `app/api/export/markdown/route.ts` reuses the existing authenticated download wrapper.
- `vercel.json` changes reminder processing from hourly to every five minutes.
- `tests/web-push-compatibility.test.ts` exercises the real package with synthetic keys and fake HTTPS transport.

Desktop retains its existing complete local export bundle and native download callbacks.
Shared summary props update from the current desktop bundle. Marketing has no
new interactions. Future mobile remains deferred. The interaction registry,
design-system example, load interaction manifest, and source-of-truth docs
record the implemented web change.

## Verification

- Required agents, interactions, resolvers, lint, typecheck, test, and build checks pass.
- Full suite on supported Node 24.19.0: 189 files passed, five environment-gated files skipped; 1,492 tests passed and 23 skipped. The production build also passes on Node 24.19.0.
- Lint has zero errors and seven existing warnings.
- The real local Supabase behavior/portability contract passes separately.
- Clean local database reset applies every migration successfully.
- Ticket 088 rollback-only SQL rejects stale freshness commits and verifies an idempotent retry.
- Ticket 091 rollback-only SQL covers 1,002 occurrences, another owner, archived behaviors, future occurrences, range validation, grants, and time-session cutoffs.
- Local RLS smoke passes 92 checks and cleans its three synthetic accounts.
- Existing time-session pagination tests retain bounded RPC transport and more-than-1,000-row coverage.
- Browser QA covers initial counts, explicit Markdown generation, and range-change reset.
- Generated Markdown fits a 390-pixel viewport without horizontal page overflow. Desktop layout passes at 1,280 pixels.
- Browser QA's two temporary local accounts were deleted by exact ID. The local QA server stopped.
- Design-system, marketing check/build, desktop typecheck/build, and desktop parity checks pass.
- Load harness: 138 Python tests pass. The manifest classifies the new Markdown HTTP interaction; no new load campaign ran.
- Public-source checks pass. Production dependency audit reports zero vulnerabilities.

## Hosted acceptance sequence

1. Obtain authorization for the additive hosted migration and one Preview deployment.
2. Verify migration history before deploying `20260905020741`; preserve the existing migration-first workflow.
3. Inspect Preview Export reads, Markdown generation, unchanged download content, and browser-push runtime logs.
4. Obtain separate Production rollout authorization. Verify reminder timing there; Preview does not execute Vercel cron.
5. For Ticket 104, obtain operator access to delivery diagnostics. Keep private provider details outside this repository.
6. Present any narrowly scoped filtering change for explicit approval after identifying the cause.
7. Obtain separate approval for one harmless synthetic email, then review the monitored Inbox, Junk/Spam, and quarantine surfaces.

Tickets 123–124 were added by concurrent planning work. This execution did not
change their scope or implement them.
